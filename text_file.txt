change #1

git status		//checks which files are being tracked

git init		//make directory a .git

git add -A		//add files to keep tracks add all changes

git commit -m		//add a message to keep track of changes like comments and 				adds a snap shot

git config —-global user.email		//adds user email to see who makes changes

git config —-global user.name		//adds username to see who makes changes

git log			//listing of all commits

git remote add origin https://github.com/henrikwok/git_testing.git

			//remote server or cloud destination to upload files to

git push -u origin master		//pushes files into destination

